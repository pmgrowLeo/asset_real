# 의존성 패키지 (용량이 크고 npm install로 설치 가능하므로 제외)
node_modules/

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 환경 변수 파일 (보안상 중요한 키가 들어갈 수 있으므로 제외)
.env

# 운영체제 관련 임시 파일
.DS_Store
Thumbs.db

# 빌드 결과물 (있는 경우)
dist/
build/